# parsefold

A small command-line utility that flattens nested key/value configuration
into dotted-path lines. Built with the Go standard library only.

## Build

```
make build
```

## Test

```
make test
```

Pull requests are validated by CI: the pipeline runs the tests and a
formatting check against the proposed change before review.
