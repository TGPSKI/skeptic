# Build artifacts
bin/
*.exe
*.test
/skeptic

# Test and benchmark output
coverage.out
bench-results.txt
bench-baseline.txt
*.sarif

# Scanner state. Per-run and per-developer, never committed.
.skeptic/
.skeptic-state.json
baseline.json

# .skeptic.json and .skeptic-waivers.json are committed here on purpose (#65).
# They gate this repository's own CI and double as the worked example users
# copy. Repositories that keep scan config developer-local should ignore them.

# Corpus artifacts
.corpus-key
corpus.lock

# Output
results/
private/
sessions/

# Profiling output
cpu.prof
mem.prof
trace.out

# OS and editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Backup files (testdata/proof/*.bak are intentional test fixtures)
cmd/**/*.bak

# Signing
*.key.pem

# Cursor

.cursor/